#ifndef ALIASES_HPP
#define ALIASES_HPP

#include <cstdint>
#include <string>
#include <vector>

using command_pair  = std::pair<std::string, uint64_t>;
using command_block = std::vector<command_pair>;

#endif // ALIASES_HPP
